import MainContainer from "../../components/containers/MainContainer"

const Settings = () => {
  return (
    <MainContainer active="settings">
        <h2>Settings</h2>
    </MainContainer>
  )
}

export default Settings
